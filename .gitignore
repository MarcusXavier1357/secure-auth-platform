# Dependências e builds
node_modules/
dist/
backend/api
backend/api.exe

# Ambiente — nunca commitar secrets
.env
*.env
!*env.example

# Chaves JWT (RS256) — nunca versionar private.pem
**/keys/private.pem
**/keys/*.pem
!**/keys/.gitkeep
